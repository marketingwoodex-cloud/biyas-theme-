{# SECTION: spacer — vertical space / divider.  data: { size:"small"|"medium"|"large", line:true } #}
<div class="rt-spacer rt-spacer-{{ s.size or 'medium' }}{% if s.line %} has-line{% endif %}" id="{{ _id }}" aria-hidden="true"></div>
